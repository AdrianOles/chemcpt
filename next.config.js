/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "static.wixstatic.com",
            "lh6.googleusercontent.com"
        ]
    }
}

module.exports = nextConfig
